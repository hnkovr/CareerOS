"""AI gateway (ADR-003, ADR-010): one provider port, vault-versioned prompts, validated
structured output, a run ledger, and renderers for external-chat bundles (Mode B) and
dev-agent task packets (Mode C).

Provider-specific code lives only in ``providers/``.
"""
