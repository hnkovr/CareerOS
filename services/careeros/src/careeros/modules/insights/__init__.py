"""Insights context (P3 home of analytics/strategy). First resident: the notification center —
computed, not stored: notifications derive from live state and disappear when the state is handled
(brief §41: no notification spam, nothing to mark as read twice)."""
