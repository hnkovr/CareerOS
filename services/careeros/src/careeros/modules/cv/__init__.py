"""CV-as-Code engine (ADR-006, ADR-010).

facts → positioning → channel rules → [JD context] → deterministic selection → AI rewriting
(provenance-guarded, optional) → CVDocument → RenderCV → pdf / md / json.
"""
