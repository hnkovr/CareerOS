# Handoff — Universal Job Intelligence lane (careeros-d2), 2026-08-26

Resume: `cd ~/gi/@hnkovr/CareerOS && claude --resume d65a7aca-097b-4eea-ba49-6975c0845dc5`
Plan (approved by owner 2026-08-26): `~/.claude/plans/cozy-percolating-lollipop.md` — slice 1 = phases 0, A, D, E, B, C, G, H.
Spec/RFC: `docs/superpowers/specs/2026-08-26-universal-job-intelligence-design.md` · ADR-015 `docs/adr/015-public-job-url-reads.md` · ADR-016 `docs/adr/016-job-provenance-snapshots.md` (rows added to `docs/adr/README.md`; d1 owns the 017 row in the same file — stage only our lines via a filtered `git apply --cached`).
GitHub: epic #32; children #33 foundation, #34 provenance/snapshots, #35 generic, #36 RocketHunt, #37 hh read-one, #38 JustJoin, #39 discovery, #40 refresh scheduler; backlog #41 web+MCP, #42 ATS, #43 archive.today/search-recovery. Comment posted on #21. Linear: not created yet (use the bulk script with `todoist_reminder: false`).

## Decisions (frozen)
D1 read-one by user URL only (no listings, no `/api/`, no contact gate, no browser automation) · D2 provider = connector, Job = Opportunity, snapshot = OpportunityRaw, provenance = `opportunity_source` · D3 no Playwright · D4 no Redis/S3 · D5 LLM fallback only · D6 ADR 014 = d1 (assistants), 017 = d1 (workflows, landed ff9fd14 with migration c4d8e2f1a9b3); ours 015/016 · D7 Wayback + Jina enabled by default (Jina skipped for private sources) · D8 slice 1 = A–H · D9 MCP backlog · D10 additive edits only in `opportunities/service.py`; never touch `modules/ai`, `api/routers.py`, `cli.py`, `pipeline/service.py`.

## Verified facts (2026-08-26)
RocketHunt: `/{en,ru}/vacancies/<uuid>`, SSR + JSON-LD JobPosting + RSC keys; robots Allow `/`, Disallow `/api/`; ToS forbids bulk/automated access; salary = aggregator estimate; contacts gated. JustJoin: `GET /api/candidate-api/offers[/<slug>]` 200 (cursor pagination), robots Disallow `/api/`, user terms §11.3. HH API anonymous → 403. Jina anonymous 200. Reference payloads saved in the session scratchpad (`rh-vacancy.html`, `jj.json`, `jj-detail.json`, `hh.json`) — sanitise before turning into fixtures.

## State of the shared tree (uncommitted, ours)
- Done by me: enum members (`vault/enums.py` Platform += rockethunt/justjoin/website; `opportunities/enums.py` Source += rockethunt/justjoin; `platform/enums.py` SOURCE_BY_PLATFORM += 3), regenerated `career/schemas/*` + `packages/schemas/src/api.d.ts` (also carry d1's OpenAPI additions — commit together), docs: spec, ADR-015/016, README rows, `docs/platform/{rockethunt,justjoin,generic}.md`, platform README matrix + arch matrix rows.
- In progress by subagent A1 (platform pure layer): `platform/enums.py` (FetchStrategy/AccessMode/SourceRelation/SyncKind.job), `platform/sources.py`, `platform/fetch/**`, `http.py request_text`, `schemas.py` (Capabilities.read_job/access, JobPosting provenance, FieldEvidence, ReadRequest/ReadOut), `base.py` (detect/canonicalize/fetch_job/extract_job/fetch_job_api, READ_IMPL), `registry.py verify`, `connectors/generic/**`, `connectors/__init__.py`, `core/config.py` job_fetch_* settings + `config/.env.config.template`, root `pyproject.toml` import-linter, tests `tests/platform/test_{sources,fetch,extract,generic}.py` + fixtures.
- In progress by subagent A2 (domain): `opportunities/{enums,models,dedup,deps,schemas,service,router}.py`, migration `job_provenance_snapshots` chained after **c4d8e2f1a9b3**, backfill, `tests/opportunities/test_provenance.py`. Known breakage being fixed: `opportunity_source` not created by conftest `create_all` → all db tests error until fixed.
- Not mine (leave alone): `CareerOS_ClaudeCode_Master_Prompt_*.md` formatting diff; d1's `migrations/env.py`, `tests/conftest.py`, `api/routers.py`, `cli.py`, `modules/workflows/**`, `career/prompts/workflows/`, `tests/workflows/` (landed ff9fd14 — may already be clean).

## Next steps (in order)
1. When A1/A2 report: review, run `make check`-equivalent (`uv run ruff/pyright/pytest` with `CAREEROS_TEST_DATABASE_URL=…/careeros_test_d2`), fix, then commit in path-scoped commits: (a) enums + regenerated schemas (`feat(platform,opportunities): platform/source enums for rockethunt, justjoin, website (#33)`), then ping d1 to push; (b) docs (spec, ADR-015/016 + filtered README hunk, platform docs, matrices) `docs(adr,platform): ADR-015/016 …`; (c) A1 code `feat(platform): fetch strategies …(#33, #35)`; (d) A2 code + migration `feat(opportunities): provenance + snapshots (#34)`.
2. Phase A3 (agent): `PlatformSyncService.read_job/refresh` in `platform/sync.py`, `POST /api/platform/read`, `GET /api/platform/detect`, `careeros platform read|detect|refresh`, `POST /api/opportunities/{id}/refresh`, bot `_capture` URL hook (coordinate with bot lane), `PlatformSyncRun(kind=job)`, e2e tests with MockTransport, Justfile recipes.
3. Connector agents in parallel: D RocketHunt (#36; use scratchpad `rh-vacancy.html` shape, sanitise), B hh read-one (#37), C JustJoin (#38); each extends its `docs/platform/<p>.md` and adds fixtures/tests; I integrate `connectors/__init__.py` + `PLATFORMS`.
4. G discovery (#39), H snapshots/refresh/scheduler (#40); then `.claude/CLAUDE.md` layout line, TODO/status/PROMPTS-LOG (+ru), `make all` via careeros-gate agent, smart-commit, Linear issues.

## Shared-tree etiquette
main only; `git add` by explicit paths only; ping d1 (uds:/tmp/cc-socks/80628.sock, "careeros-41") and the bot lane before staging shared files; test DB `careeros_test_d2`; never `git add -A`, never commit `.claude/settings.local.json` or `.env.secrets*`.
