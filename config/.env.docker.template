# Rendered to config/.env.docker (git-ignored) and loaded by docker-compose `env_file`.
# Host-side overrides for compose (ports, vault mount). Service URLs are set inside docker-compose.yml.
API_PORT=${API_PORT:-8000}
WEB_PORT=${WEB_PORT:-3000}
POSTGRES_PORT=${POSTGRES_PORT:-5432}
REDIS_PORT=${REDIS_PORT:-6379}
POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-careeros}
CAREEROS_VAULT_HOST_PATH=${CAREEROS_VAULT_HOST_PATH:-./career/private}
CAREEROS_ENV=${CAREEROS_ENV:-dev}
CAREEROS_LOG_JSON=${CAREEROS_LOG_JSON:-true}
CAREEROS_USER_EMAIL=${CAREEROS_USER_EMAIL:-demo@careeros.local}
CAREEROS_AI_DEFAULT_PROVIDER=${CAREEROS_AI_DEFAULT_PROVIDER:-anthropic}
CAREEROS_ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
CAREEROS_OPENAI_API_KEY=${OPENAI_API_KEY:-}
CAREEROS_API_TOKEN=${CAREEROS_API_TOKEN:-}
