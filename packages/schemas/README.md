# @careeros/schemas

Generated TypeScript types (ADR-009). `src/api.d.ts` is produced from the FastAPI OpenAPI document
by `npm run generate` at the repo root — never edit it by hand. The JSON Schemas for career YAML
live in `career/schemas/` (generated by `just export-schemas`).
