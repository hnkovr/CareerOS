# career/private

Default location of **your** private career vault. Everything here except this file is git-ignored.

Initialise: `just vault-init` (copies the scaffold from `career/templates/` and `git init`s it), or
point `CAREEROS_VAULT_PATH` at an existing private repository instead.
