# Prompts log

| Date | Summary | Outcome |
|---|---|---|
| 2026-08-20 | Founder brief: build CV-as-Code / Career Data Platform (CareerOS). Full text preserved in `docs/product/00-product-brief.md`. Asked for: architecture analysis → bounded contexts → canonical vs operational → domain model → monorepo → capabilities matrix → P0–P3 → risks → ADRs → P0 implementation. | Architecture docs + ADR 001–010 committed; P0 slices started. |
| 2026-08-25 | Platform connectors: create submodules to read own profile, search jobs and check application statuses for Upwork, Wellfound, LinkedIn, Indeed, getmatch, Toptal, hh.ru (scripts/skills/sub-agents, parallel agents); mid-turn: upsert related tests (& debug), docs, GHIs, Linear project. | `modules/platform` core (contract, registry, tokens/OAuth, sync, API, CLI, just) + 7 connectors built by parallel sub-agents; ADR-013; docs/platform; agents `careeros-platform-{ops,connector-dev}`; GitHub #10–#21; Linear MY-26…MY-37 (project CareerOS). |
| 2026-08-25 | `fix/refactor/extend/optimize and run <make all>` — the draft `all:` target (`test lint test fmt test bot-check seed run`) had duplicate prerequisites, ran `fmt` after `lint`, and pulled in `run` (docker `--build` + `open .`). | Makefile reworked (ordered 12-step `all`, new `check`/`infra`/`build`/`distclean`, step banners, `.NOTPARALLEL`, `clean` no longer deletes OAuth tokens); two blocking bugs fixed: `platform sync all` reported not-connected as FAILED (exit 1 on a fresh install) and the vault default had no demo fallback, so `validate-career`/`generate-cv` failed; +2 tests, docs updated. `make all` → exit 0 (523 py + 5 web tests, ruff/pyright/import-linter clean). |
| 2026-08-26 | "go on" ×2 — continue roadmap work while P1.3 Gmail stays blocked; chose the smaller P3 items over §55 tool-calling (needs an ADR + go-ahead). Mid-session: the platform lane handed over two CI failures (pgvector extension missing in the search migration; api.d.ts drift). | Drift detection + daily brief (`0f7383e`), insights market/skills-gap/funnel (`58d5370`), per-platform checklists (`33b8eef`), pgvector migration fix (`f23b677`, verified on a fresh DB; contracts already clean), interview/negotiation intelligence + AI-ranked comparison. |
| 2026-08-25 | `fix/refactor/extend/optimize and run <make all>` (round 2, pipeline already green) | Closed three gaps the green pipeline was hiding: CI's "schemas are fresh" step diffed `packages/schemas` but only ever regenerated `career/schemas`, so the TS API contract could drift silently (`scripts/contracts-check.sh` + `contracts` CI job + `make contracts`, negative-tested); `make all` failed outright when offline (tg-bot.sh now exits 4 for unreachable vs 2 for a rejected token — `bot-check` tolerates only 4); the web half of the gate skipped silently without `node_modules` and now says so. CI also builds `Dockerfile.web`. `make all` → exit 0. |
| 2026-08-26 | `fix all problems, upsert test & docs & GHIs, skills/agents/hooks, run <make all> again, merge branch` | `make all` was red in three places: `Settings()` could not build from a rendered `.env` ([#22](https://github.com/hnkovr/CareerOS/issues/22) — blank `int \| None`), an unfixable long line, and 54 pyright errors in the bot lane's new tests. Fixed all three; added `config/gate.yml` (pipeline as data) + `tests/test_gate_config.py`, `scripts/hooks/config-guard.sh`, skill `/careeros-gate`, agent `careeros-gate`; filed [#22](https://github.com/hnkovr/CareerOS/issues/22)/[#23](https://github.com/hnkovr/CareerOS/issues/23). `make all` → exit 0 (590 py + 5 web tests). `feat/platform-connectors` verified fully merged (0 unique commits). |
