# Журнал промптов

| Дата | Кратко | Результат |
|---|---|---|
| 2026-08-25 | Коннекторы платформ: сабмодули для чтения своего профиля, поиска вакансий и проверки статусов откликов для Upwork, Wellfound, LinkedIn, Indeed, getmatch, Toptal, hh.ru (скрипты/скиллы/саб-агенты, параллельные агенты); в процессе: тесты (и отладка), документация, GHI, проект в Linear. | Ядро `modules/platform` (контракт, реестр, токены/OAuth, синк, API, CLI, just) + 7 коннекторов параллельными саб-агентами; ADR-013; docs/platform; агенты `careeros-platform-{ops,connector-dev}`; GitHub #10–#21; Linear MY-26…MY-37 (проект CareerOS). |
| 2026-08-25 | `fix/refactor/extend/optimize and run <make all>` — черновая цель `all:` (`test lint test fmt test bot-check seed run`): дублирующиеся зависимости, `fmt` после `lint`, внутри `run` (docker `--build` + `open .`). | Makefile переработан (упорядоченный `all` из 12 шагов, новые `check`/`infra`/`build`/`distclean`, баннеры шагов, `.NOTPARALLEL`, `clean` больше не удаляет OAuth-токены); исправлены две блокирующие ошибки: `platform sync all` помечал «не подключено» как FAILED (код 1 на свежей установке) и у хранилища не было отката на демо-vault, из-за чего падали `validate-career`/`generate-cv`; +2 теста, документация обновлена. `make all` → код 0 (523 py + 5 web тестов, ruff/pyright/import-linter чисто). |
| 2026-08-25 | `fix/refactor/extend/optimize and run <make all>` (2-й заход, пайплайн уже зелёный) | Закрыты три пробела, которые зелёный пайплайн скрывал: в CI шаг «схемы свежие» сравнивал `packages/schemas`, но регенерировал только `career/schemas` — TS-контракт API мог тихо разъезжаться (`scripts/contracts-check.sh` + джоба `contracts` + `make contracts`, проверено негативным тестом); `make all` падал без сети (tg-bot.sh теперь код 4 = недоступен против 2 = токен отвергнут, `bot-check` терпит только 4); веб-часть гейта молча пропускалась без `node_modules` — теперь сообщает. CI также собирает `Dockerfile.web`. `make all` → код 0. |
| 2026-08-26 | `fix all problems, upsert test & docs & GHIs, skills/agents/hooks, run <make all> again, merge branch` | `make all` падал в трёх местах: `Settings()` не собирался из отрендеренного `.env` ([#22](https://github.com/hnkovr/CareerOS/issues/22) — пустой `int \| None`), неисправимая длинная строка и 54 ошибки pyright в новых тестах бот-линии. Всё исправлено; добавлены `config/gate.yml` (пайплайн как данные) + `tests/test_gate_config.py`, `scripts/hooks/config-guard.sh`, скилл `/careeros-gate`, агент `careeros-gate`; заведены [#22](https://github.com/hnkovr/CareerOS/issues/22)/[#23](https://github.com/hnkovr/CareerOS/issues/23). `make all` → код 0 (590 py + 5 web тестов). `feat/platform-connectors` — полностью влита (0 своих коммитов). |
| 2026-08-26 | `fix all problems, upsert test & docs & GHIs, skills/agents/hooks, run <make all> again, merge branch` (2-й заход) | Закрыты две открытые проблемы гейта: [#24](https://github.com/hnkovr/CareerOS/issues/24) — изоляция тестов (autouse-truncate после каждого db-теста; проверка дедупликации зависела от порядка) и [#23](https://github.com/hnkovr/CareerOS/issues/23) — расхождение CI и локального гейта (`scripts/gate.sh` — единственное определение, проверяется `tests/test_gate_config.py`). `just infra-up` подсказывает, что делать, если Docker не запущен. Ветки для слияния нет — остался только `main`, синхронный с origin. |
| 2026-08-26 | `upsert </cv improve> & other tg-bot commands` — доделать CV- и query-команды бота после компакции. | `35898ba`: `/queries` выводится из позиционирований волта (детерминированно, адресуется по номеру через `/urls <n>`), `/cv update` (без AI, присылает PDF), `/cv improve` (новый `CVService.improve` — AI-проход сравнивается со **свежей** базовой генерацией по дословным фактам, `derived_from[]` не теряется). Попутно исправлены три скрытых бага: невалидный MarkdownV2 в `/help`, отсутствие разбиения сообщений по лимиту 4096, блокирующее чтение файла в event loop. Добавлен 6-й контракт import-linter для бота. GH [#25](https://github.com/hnkovr/CareerOS/issues/25)/[#27](https://github.com/hnkovr/CareerOS/issues/27)/[#28](https://github.com/hnkovr/CareerOS/issues/28)/[#29](https://github.com/hnkovr/CareerOS/issues/29)/[#30](https://github.com/hnkovr/CareerOS/issues/30) закрыты; в Linear заведены MY-38…MY-42 на остаток. Гейт зелёный, включая реальный Postgres. |
| 2026-08-26 | `do this your way` — закрыть открытый вопрос про aiogram и действовать. | **Решено: aiogram не берём**, остаётся тонкий httpx-клиент; зафиксировано в спеке в разделе *Decision reversals* (и исправлена прежняя ошибка атрибуции — ADR 012 библиотеку никогда не называл). Дальше сделано то, что этим решением блокировалось: [#4](https://github.com/hnkovr/CareerOS/issues/4) триаж — `/next`, `/top [n]`, `/opp <handle>` плюс кликабельная форма `/opp_<id>`, и четыре inline-кнопки через `opportunities.service`. Callback отвечается ДО работы (иначе кнопка крутится), неизвестный payload отклоняется, а не игнорируется. 33 новых теста; 220 bot + 49 deploy. Гейт зелёный, включая реальный Postgres. |
| 2026-08-26 | `Prepare repo for migration to another workstation PC - Macbook M1' not found` → `upsert scripts/settings/skills/agents/docs/GHIs for this 1st` | Собрана линия переезда до самого переезда (M1 в сети не найден). `config/workstation.yml` (порядок бутстрапа с `proves`, что переезжает и что нет — с указанием, как это восстановить, имена секретных слотов, ловушки с командой-`proof`); скрипт вердикта `make preflight`; SessionStart-хук `workstation-guard.sh`; `.ai/workstations/macCoreI9.yml`; `docs/runbooks/new-workstation.md`; `tests/test_workstation_config.py` (9 проверок соответствия — ловят случай, когда новый секрет добавили, а документ переезда об этом не узнал); скилл `/careeros-workstation`; агент `careeros-workstation`; регистрация в `careeros.yml` и `locations.yml`. Реальный вердикт preflight: **blocked** — у волта нет remote, хранилище Bitwarden заперто, есть незакоммиченное. GH [#44](https://github.com/hnkovr/CareerOS/issues/44)–[#49](https://github.com/hnkovr/CareerOS/issues/49). |
